= form_for @question, { remote: (true if action_name != 'new'), html: { id: "edit-question-#{@question.id}" } } do |f|
  = f.label :title
  = f.text_field :title
  = f.label :body
  = f.text_area :body
  = f.submit 'Save'