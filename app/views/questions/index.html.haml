- @questions.each do |q|
  %p= q.title
  - if current_user && current_user.id == q.user_id
    %p= link_to 'Delete question', question_path(q), method: :delete

%p= link_to 'Ask question', new_question_path
