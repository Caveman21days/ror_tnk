.errors

%h3= @question.title
%p= @question.body


.answers
  = render @question.answers


- if user_signed_in?
  = form_for [@question, @answer], remote: true do |f|
    = f.label :body, 'answer'
    = f.text_field :body
    = f.submit 'Submit'
- else
  %p please sign in/sign up for answer
